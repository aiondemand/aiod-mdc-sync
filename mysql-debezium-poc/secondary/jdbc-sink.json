{
  "name": "mysql-sink",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",

    "topics": "primary.test_db.items",

    "connection.url": "jdbc:mysql://secondary-mysql:3306/test_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC",
    "connection.user": "test_user",
    "connection.password": "test_pass",

    "insert.mode": "upsert",
    "pk.mode": "record_key",
    "pk.fields": "id",
    "delete.enabled": "true",
    "behavior.on.null.values": "delete",
    "auto.create": "true",
    "auto.evolve": "true",

    "transforms": "extract,route",
    "transforms.extract.type": "org.apache.kafka.connect.transforms.ExtractField$Value",
    "transforms.extract.field": "after",
    "transforms.route.type": "org.apache.kafka.connect.transforms.RegexRouter",
    "transforms.route.regex": "^primary\\.test_db\\.(.*)$",
    "transforms.route.replacement": "$1",

    "transforms.ts_created.type": "org.apache.kafka.connect.transforms.TimestampConverter$Value",
    "transforms.ts_created.field": "created_at",
    "transforms.ts_created.target.type": "Timestamp",
    "transforms.ts_created.format": "yyyy-MM-dd'T'HH:mm:ssX",

    "table.name.format": "${topic}"
  }
}
